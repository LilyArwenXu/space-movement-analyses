# Inclusive Vitality：数据和计算口径

## 当前口径（2026-09-10，取代下方历史算法）

当前8个宏观页面共用全量总表(0907)的135个点位，源工作簿保持不变。按序号与完整地址核验后关联地图点位ID，同址不同观测不合并。实现入口为 `scripts/final_revision.py`，五维图表及页面入口为 `aerial/assets/js/final-revision.js`；它们接入原构建流程，重新构建不会丢失本次修改。

五维频数直接读取全量总表：年龄 AM:AP，活动 AV:BI（14项，BB饮食与BC休憩分开），社交 AK:AL，姿态 AT:AU，身份倾向 BS:BT。身份倾向使用居民指数、游客指数作为相对权重，不比较行人身份后重新计数。H_d=−Σ(p_k ln p_k)/ln K_d，p_k为各类别值/维度合计，0 ln 0记0；任一输入缺失或该维合计为0，则该维与综合评分缺失。

M严格按指定公式计算：0.149H年龄＋0.141H活动＋0.250H身份倾向＋0.228H姿态＋0.233H社交状态。系数表征中给定的w3/w4/w5名称与公式的维度顺序不同，图片与权重说明按原要求保留，页面明确注明实际公式顺序；不私自调整权重或归一化其和。

Q采用11项指标，依次为有效停留面积、可承载停留人数、消费型停留空间占比、遮荫率、声环境舒适度、气味环境、视觉丰富度、历史感知度、路面状态、界面开放度、临界互动性；对应权重0.068、0.078、0.167、0.112、0.069、0.058、0.095、0.099、0.077、0.094、0.084。

V采用人数、行人集群数、停留密度、聚集比例，权重依次为0.194、0.144、0.205、0.456。停留密度=人数/有效面积，聚集比例=集聚人数/人数。Q与V均逐指标按全域有效数据计算z=(x−min)/(max−min)，常量记0，再求Σ(wz)；缺一项则综合评分缺失。删除旧的人为展示放大系数，居民/游客指数不再进入V的底层展示。

三类不配得性坐标为(Q,V)、(Q,M)、(V,M)，均直接引用总表最终评分，以0.5为高低界限，边界归高组。只纳入左上与右下两类点位；`data-extract.csv`与兼容文件`data_extract.csv`包含同样的173条分类记录（点位可属于多个分析）。后续分析图表按要求暂不制作，仅呈现选项卡、副标题及对应点位。

数据缺失情况：Q有12个、V有30个、M有24个点位不评分。`python scripts/verify_final_data.py`独立核验源字段、熵、Q/V/M和分类一致性。运行原构建脚本更新页面，再运行`node scripts/export_macro_png.cjs`导出所有当前选项卡；`result/manifest.json`记录37张300 dpi PNG与37个末级选项卡视图。无图的六个不配得性分析选项卡也纳入清单，避免将有意留空误作漏导。

## 历史口径存档（以下内容不再决定当前评分和页面）

网站所有当前图表共用 `aerial/assets/data/inclusive-data.js`。输入是原始工作簿、地图包及目录PDF；工作簿不会被改写。运行 `scripts/build_site.py` 刷新数据、当前九个图表、发布目录和 `data_extract.csv`。筛选表保存在项目根目录，不在网页提供下载或提示。

## 数据单元

以总表的点位ID为单元，地图135点逐一匹配。相同地址的不同ID不合并。行人表以点位ID汇总，目前594条行人记录。地图的活动数及活力度计数使用总表；五维构成使用行人表。两表若更新不同步，各页仍保持其明确的数据口径。

全量总表和行人信息总表若存在精确同名表则优先读取，否则读取0906/0905版本。当前相关矩阵使用24个界面/空间指标与11个人群指标。B1编码a=0、c=1、b=2、d=3；B2编码a/b/c/d=0/1/2/3；B3a/b/c分别统计对应子类数；C1–C4编码1–4；D附属设施加权指数为已记录D1–D7不同类别数/7（等权）。这些是分析编码假设，整项空缺保留缺失。

## 评分（本次等权建模方案）

原始活力度 V₀：总表行人样本数、行为集群数、停留密度、集聚比例四项分别作全域最小最大标准化，取等权平均。停留密度=人数/有效面积，集聚比例=集聚人数/人数。常量指标标准化为0；缺任一项则不评分。四项都按越高越活跃的正向指标处理。

展示活力度 V=min(1,c×V₀)。令 t 为 M≥0.5 且 V₀>0 的点位中第8大V₀（不足8个则取最小正值），c=max(1/max(V₀),0.500001/t)。当前c=1.45748987854251，双高11个。这是按用户要求使评分铺展并保留至少8个双高点的展示校准，不是独立验证的统计模型；更新数据后系数与筛选结果都会变化。居民/游客指数仅展示，不计入V。

界面品质仅筛选空间指标中存在ρ>0且p<0.05的相关对，同一空间指标只计一次。用户确认采用“正相关且显著”，不称为强相关。当前入选消费型停留空间占比与界面开放度，相关系数约0.20–0.25。Q为两项全域最小最大标准化值的等权平均，缺任一项则不评分。

混合度 M：年龄4类、活动14类、身份倾向2类、姿态2类、社交状态2类，各按 H=−Σ(p ln p)/ln K 计算，五项等权平均。活动允许多选，以活动出现次数为该维分母。居民/游客身份倾向通过行人表两个指数的大小比较；相等或缺失不计入该维。没有有效类别时该维缺失，综合评分也缺失。n<5仍显示，图注明确样本波动，不把结果作为稳定估计。

不配得性 U=ln[(1+V)/(1+M)]。V=M时U=0，V>M时为正，V<M时为负。加1避免零值导致对数无定义，范围为[−ln2,ln2]。这衡量的是两项分数的差距，双高或双低但不相等时仍可不为0。

四象限统一以0.5作为两轴高低阈值。`data_extract.csv` 包含 V≥0.5 的有效评分节点，并分为 M<0.5 的“高活力低混合”和 M≥0.5 的“双高”；边界0.5归高组。以全精度计算和筛选，网页只对显示数值四舍五入。数据更新后每次构建重新生成筛选表。

## 相关性和趋势

Spearman逐对排除缺失、并列值取平均秩，系数与p由SciPy计算，双侧近似检验，未做FDR或其他多重校正。居民/游客指数按点位有效均值汇总；不再引用旧HTML报告中的固定系数。编码方式会影响分类指标的相关结果。全部缺失或常量指标不产生系数。

空间散点图采用OLS整体回归，R²≥0.5画实线，其余虚线，恒定因变量的R²不定义。不配得性分析仅绘制高活力低混合点位，按用户列出的五组空间指标绘制散点，不连接为趋势线。

## 地图与记忆

地图始终保持6144:4340原始比例，坐标为配套底图百分比。多层圆按人数平方根调整半径。热图为人数加权高斯核叠加，σ为底图宽度1.8%，归一化后冷暖色显示相对强弱；它不是每平方米的绝对人数。只有地图将缺失人数显示为0，评分不填0。

“在地记忆”沿用活动→功能的明确优先规则，当前老年/中年记录逐人映射，连线总数等于选定年龄群体人数。这是代际偏好假设，不是真实历史用途或固定20年回溯。

## 页面与历史文件

当前界面、图表和表格直接使用统一浅色样式；目录PDF渲染成灰度页图，不使用会改变界面的第三方文件查看器。外部“调研网页”在新标签打开，其网站样式由外部站点控制。

旧图表文件保留，旧深色绘图用单独反色样式保持浅色显示。新版编辑入口是 `chart_python/current`；父目录14份Python保留为历史备份，防止误运行覆盖新版活动页面。

## 0907源表适配与回归交互
固定读取全量总表（0907）与行人信息总表（0907）。新版点位序号与地图序号、完整地址同时核对；行人按地址关联，重复地址按当前表中点位内人员序号重新起算的观测批次、总表点位顺序关联，不读取旧工作表。当前所有观测来自0907表。D附属设施加权指数直接使用新版总表该列的已计算值。
不配得性Ⅲ各面板采用OLS回归，R²≥0.5为深粉色实线，其余浅粉色虚线；少于3个有效点、横轴恒定或文字指标不拟合。品质散点回归条带为拟合均值±k×SE，SE=s√[1/n+(x−x̄)²/Σ(xᵢ−x̄)²]，s²=残差平方和/(n−2)。滑条k范围0–4，不代表固定置信水平。活力度叠加图使用7个独立纵轴，保留原始指标数值，不能跨指标比较绝对高度。


## 0908信息表与案例
居民、游客的点位均值直接读取“点位均值”，相同完整地址使用该表给定均值。界面品质保持正向显著空间指标标准化后等权平均的公式；当前入选指标由当前数据重新计算。
案例分析仅使用当前点位文件夹下的节点与行人CSV。节点记录为统计样本；行人按点位ID关联，用于计算年龄、行为、姿态和社交构成的标准化Shannon熵，不重复节点观测。活力度原始指标直接读取节点人数、集群数，并计算人数/有效面积和集聚人数/样本人数；不得引用工作簿的全域归一化边界或校准系数。当前每个案例仅有一条节点记录，Spearman不可估计，活力度综合评分也缺乏案例内标准化范围。CSV没有居民/游客指数，身份混合度与混合度综合评分保留缺失。散点图只绘制可计算的案例指标，使用颜色区分并列展示，横向5px级视觉错位用于防止遮挡，不改变原始读数；不进行跨单位数值优劣比较。单节点不绘制回归和显著性异常圈。
